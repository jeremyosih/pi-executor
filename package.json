{
  "name": "pi-executor",
  "version": "0.1.0",
  "type": "module",
  "keywords": ["pi-package"],
  "files": [".pi", "src", "dist", "README.md"],
  "pi": {
    "extensions": [".pi/extensions/pi-executor"]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@sinclair/typebox": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build --silent && node --test dist/test/*.test.js"
  }
}
